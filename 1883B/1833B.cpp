#include <bits/stdc++.h>
#define ll long long;
using namespace std;
void fun(){
    int n,k;
    cin >> n >> k;
    string a;
    cin.ignore();
    cin >> a;
    int sum = 0;
    while (sum > )
}
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int x;
    cin >> x;
    while(x--){
        fun();
    }
    return 0;
}